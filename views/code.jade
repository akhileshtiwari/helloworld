doctype
html
	head
		title Code
	body
		p
			| With jade we can also have inline code. For instance, Let's set some var and based on condition do one thing or another.

		- var env = 'dev';

		- if(env == 'dev'){
			p We are in dev environment
		- } else{
			p We are in prod environment
		- }

		p And some more text.